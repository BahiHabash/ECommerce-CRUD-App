<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto',
          'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans',
          'Helvetica Neue', sans-serif;
        -webkit-font-smoothing: antialiased;
        background-color: #f4f4f7;
      }
      .container {
        width: 90%;
        max-width: 600px;
        margin: 20px auto;
        background-color: #ffffff;
        border-radius: 12px;
        border: 1px solid #e0e0e0;
        overflow: hidden;
      }
      .header {
        background-color: #007bff; /* Primary blue color */
        padding: 30px 40px;
        text-align: center;
      }
      .header h1 {
        margin: 0;
        color: #ffffff;
        font-size: 28px;
        font-weight: 600;
      }
      .content {
        padding: 40px 40px 30px;
        line-height: 1.7;
        color: #333333;
        font-size: 16px;
      }
      .content p {
        margin-top: 0;
      }
      .button-container {
        text-align: center;
        margin: 30px 0;
      }
      /* Bulletproof Button */
      .button {
        display: inline-block;
        background-color: #007bff;
        color: #ffffff;
        font-size: 16px;
        font-weight: 600;
        text-decoration: none;
        padding: 14px 28px;
        border-radius: 8px;
        /* iOS link fix */
        color: #ffffff !important;
        text-decoration: none !important;
      }
      .warning {
        font-size: 14px;
        color: #555555;
      }
      .footer {
        background-color: #f9f9f9;
        color: #888888;
        padding: 30px 40px;
        text-align: center;
        font-size: 12px;
        border-top: 1px solid #e0e0e0;
      }
      .footer p {
        margin: 5px 0 0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Password Reset Request</h1>
      </div>
      <div class="content">
        <!-- Use a placeholder if username isn't available -->
        <p>Hello<% if (locals.username) { %> <%= username %><% } else { %> there<% } %>,</p>
        <p>
          We received a request to reset the password for your account associated
          with this email address.
        </p>
        <p>
          If you made this request, please click the button below to set a new
          password. This link will expire in 15 minutes.
        </p>

        <div class="button-container">
          <!-- The EJS variable for the reset URL -->
          <a href="<%= passwordRestUrl %>" target="_blank" class="button">
            Reset Your Password
          </a>
        </div>

        <p class="warning">
          <strong>Security Warning:</strong> If you did not request a password
          reset, please ignore this email or contact support. Do not forward this
          email.
        </p>

        <p>Thanks,<br />The ECommerce App Team</p>
      </div>
      <div class="footer">
        <p>&copy; <%= new Date().getFullYear() %> ECommerce App. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
